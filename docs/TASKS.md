# Задачи команды

## Али — `feat/core`

1. Создать минимальное Next.js/TypeScript приложение.
2. Описать типы `Contractor`, `MatchRequest`, `MatchResult`, `RejectionSummary`.
3. Загрузить официальный CSV на сервере.
4. Реализовать строгие фильтры и детерминированный score с явным tie-break по `id`.
5. Подключить endpoint поиска, Vercel и финальный merge.

## Даурен — `feat/ui`

1. Форма: город, дата, тип, категория, бюджет; опционально язык и часы.
2. Три понятных состояния результата из CASE.
3. Карточки с именем, категорией, городом, ценой, признаками synthetic/imputed и объяснением.
4. Loading, ошибка API, повтор запроса, мобильная проверка.

## Димаш — `feat/ai-data`

1. Проверить парсинг 66 профилей, категории и даты.
2. Подготовить три обязательных demo-запроса и ожидаемые результаты.
3. Реализовать шаблонные конкретные объяснения; затем опциональный LLM rewrite без изменения фактов и порядка.
4. Тесты детерминизма, разных дат, бюджета, языка, длительности и пустых результатов.
5. README, архитектура и сценарий демонстрации.

## Рубежи

- 14:00: приложение запускается, CSV читается, форма отправляется.
- 15:00: строгая фильтрация и три исхода работают.
- 16:00: детерминированное ранжирование и конкретные объяснения.
- 17:00: три demo-запроса, тесты, deploy и README.
- 18:00: финальная проверка и сдача.
