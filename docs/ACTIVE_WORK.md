# Активная работа — изменяет только Али

Перед любой новой задачей каждый участник выполняет `git fetch origin`, читает этот файл и пишет Али. AI не начинает исправление без назначенного владельца.

| ID | Проблема/задача | Владелец | Ветка | Статус | Основание |
|---|---|---|---|---|---|
| CORE-1 | Каркас, CSV loader, matching API | Али | `feat/core` | DONE | `cd577f9` |
| UI-1 | Форма и карточки | Даурен | `feat/ui` | MERGED | интегрировано в `f52a9f3` |
| BUG-1 | Бюджет 1 500 000 не проходил HTML step validation | Али | `feat/core` | DONE | `f52a9f3` |
| DATA-1 | Demo fixtures и проверки датасета | Димаш | `feat/ai-data` | ACTIVE | ожидается commit/PR |
| DEPLOY-1 | Vercel deployment | Али | `feat/core` | TODO | после синхронизации команды |
| AI-1 | Опциональная LLM-переформулировка фактов | Димаш | `feat/ai-data` | TODO | только после DATA-1 |

Статусы: `TODO`, `ACTIVE`, `REVIEW`, `DONE`, `MERGED`, `BLOCKED`.

## Протокол бага

1. Нашедший баг не чинит его автоматически. Он отправляет Али: шаги, ожидаемое и фактическое поведение, ветку/SHA.
2. Али проверяет, нет ли этой проблемы в таблице и в свежем `origin/main`.
3. Али назначает одного владельца и меняет статус на `ACTIVE`.
4. Остальные AI получают явную команду: «не менять файлы этой задачи».
5. После merge Али ставит `DONE/MERGED` и указывает SHA.
6. Перед началом следующей задачи участник синхронизируется с `origin/main`.

Если два участника одновременно нашли один баг, второй становится проверяющим: воспроизводит исправление владельца, но не пишет альтернативный patch.
